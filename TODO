- Upload to google play:
  * Sign apk
  * Icons and images
- Money handling: 
  * Allow to drop an arbitrary amount of money, not just a single coin
  * Support for money as object on section
  * Allow to pick droped money
  * Until that, remove the option to drop a single coin...
- Save games is not working on safari
- RE-TEST the random table
- Death UI should be located after the choices, not before (spanish deadends)
- If you pick an object on the current section: 
  * The rules conditioned by "hasObject" (or "meal" rules) should be re-executed
  * Update combats combat ratio UI
- ERROR: Hunting STILL activated on book 3...
- If the book load fails, it doest not try to reload the next time
- Test all books / all sections rendering. It should be valid HTML5
  (https://validator.w3.org/docs/api.html)
- Test tags <ch.* /> replacements
- Test controller: Test rules
- Find "TODO" and do it
- Android application:
  * App lifeciclye
  * Load games page: Show info about saved games
  * Load games page: Remove ".json" extension
  * Replace toastr by Android toasts ???
  * Remove animations from modal dialogs (only for Android, performance...)
- Document all rules
- Combats should be sequential: First finish the first one, then the second, etc
- Toasts with images: align text when the text is multiline
- About the book: 
  * Add the title section info (authors info, and more)
- Save the scroll position on the current section when you change of page
- If the hunting is disabled on the current section, show it on some place (Action Chart?)
- Implement phisical random tables (pick on the screen) ???
- Use project aon colors ???
- Book 2 sect290, there are spanish / english inconsistencies
- Add robots file, description tags, etc for Web Search. Search Console?. 
  * Register visits to setup controller (new games / continue game)
- Letter for death on combats: "M" for Spanish / "K" for English
- Common performance (Android and web):
  * Rendering performance on book 2 / sect equimpnt
- Book 5 / sect400: Report differences: On part I you can loss all your inventory and
  do not recover it. On sect400 (english)  you always can recover sommerswerd. On
  spanish version not
- Performance: Do not load automatically book 1 cover on startup (see network on f12)
- UI: All buttons should be width 100%

Reminders:

* DON'T BE RESTRICTIVE WITH CHOICES !!!!

* JsDoc docs: http://usejsdoc.org/

* Build project AON xhtml:
  cd [trunk or tag directory]
  export AONDIR=`pwd`
  export LANGS=[laguage ("en" or "es")]
  ./common/scripts/build-xhtml.sh

* Bug with cordova android icons:
  http://stackoverflow.com/questions/40351434/cordova-android-6-4-0-creates-res-folder-top-level-not-inside-platforms-android

* Upload code to github:
  git remote add origin https://github.com/tonib/kaichronicles.git
  git push -u origin master

* Create new tag:
  git tag <tagname>
  git push origin --tags
  
* Android AVD
  cd [android-sdk]
  tools/android avd

* node.js
  Install dependencies: npm install
  Run script: npm run lint
  

Work done on this commit:
